# Скопируйте в .env и правьте под своё окружение.
# Файл .env в git не попадает, .env.example — попадает.

# true — работать на мок-данных (без бэкенда), false — ходить в реальный API
VITE_USE_MOCK=true

# Базовый URL API
VITE_API_BASE_URL=/api
